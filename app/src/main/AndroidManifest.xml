<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spm1">

    <application
        android:allowBackup="true"
        android:icon="@drawable/spmlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/spmlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Utils.list_parcel"></activity>
        <activity android:name=".UI.HistoryParcelsActivity" />
        <activity android:name=".UI.AddParcelActivity" />
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>